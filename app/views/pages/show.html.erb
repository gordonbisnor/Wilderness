<%= title(@page.title,:h1) %>

<% unless @page.disabled_for_comments? %>
	(<%= link_to_function pluralize(@page.comments.ham.length,'comment'), 'new Effect.ScrollTo("comments")' %>)<br />
<% end %>

<%= @page.content %>

<%= comments_for @page %>